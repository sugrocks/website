sudo: false
dist: trusty
language: node_js
node_js:
    - "node"
python:
    - "3.6"
before_install:
    - python -V
    - pip -V
install:
    - npm install --only=dev
    - pip install flake8
script:
    - npm test
    - flake8 .
